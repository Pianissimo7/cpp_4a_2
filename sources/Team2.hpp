#include "Team.hpp"

using namespace std;

namespace ariel {
    class Team2 : public Team {
        public:
            Team2(vector<Character> team, Character& leader) : Team(team, leader) {};
        private:
    
    };
}